DATABASE_URL=sqlite+aiosqlite:///./dev.db
# Required — replace with a random 32+ character secret (e.g. openssl rand -hex 32)
JWT_SECRET=replace_with_your_own_secret_here
ADMIN_EMAIL=admin@example.com
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_USER=
SMTP_PASSWORD=
